services:
  db:
    image: postgres:17-alpine
    env_file:
      - ../.env
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-ege_grader}
      POSTGRES_USER: ${POSTGRES_USER:-ege}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-ege_dev}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER:-ege} -d ${POSTGRES_DB:-ege_grader}",
        ]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build:
      context: ..
      dockerfile: dev/Dockerfile
    env_file:
      - ../.env
    environment:
      DATABASE_URL: postgresql+psycopg://${POSTGRES_USER:-ege}:${POSTGRES_PASSWORD:-ege_dev}@db:5432/${POSTGRES_DB:-ege_grader}
      LLM_PROVIDER: ${LLM_PROVIDER:-fake}
    ports:
      - "${APP_PORT:-8000}:8000"
    volumes:
      - ..:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile.dev
    env_file:
      - ../.env
    environment:
      CI: "true"
      VITE_DEV_PROXY_TARGET: http://app:8000
    ports:
      - "${FRONTEND_PORT:-5173}:5173"
    volumes:
      - ../frontend:/app
      - frontend_node_modules:/app/node_modules
    command: sh -c "pnpm install --frozen-lockfile && pnpm dev --host 0.0.0.0 --port 5173"
    depends_on:
      - app

volumes:
  pgdata:
  frontend_node_modules:
